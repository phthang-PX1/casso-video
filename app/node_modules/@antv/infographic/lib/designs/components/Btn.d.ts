import type { RectProps } from '../../jsx';
export interface BtnProps extends RectProps {
    indexes: number[];
}
export declare const BtnAdd: (props: BtnProps) => any;
export declare const BtnRemove: (props: BtnProps) => any;
