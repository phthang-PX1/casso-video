import type { WhisperWebLanguage, WhisperWebModel } from './constants';
import type { LogLevel } from './log';
import type { TranscriptionItemWithTimestamp, TranscriptionJson } from './result';
declare global {
    interface Window {
        remotion_wasm_moduleOverrides?: Record<string, (...args: any[]) => void>;
    }
}
export type TranscribeParams = {
    channelWaveform: Float32Array;
    model: WhisperWebModel;
    language?: WhisperWebLanguage;
    onProgress?: (p: number) => void;
    onTranscriptionChunk?: (transcription: TranscriptionItemWithTimestamp[]) => void;
    threads?: number;
    logLevel?: LogLevel;
};
export declare const transcribe: ({ channelWaveform, model, language, onProgress, threads, onTranscriptionChunk, logLevel, }: TranscribeParams) => Promise<TranscriptionJson>;
