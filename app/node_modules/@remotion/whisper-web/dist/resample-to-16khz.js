"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.resampleTo16Khz = exports.EXPECTED_SAMPLE_RATE = void 0;
const log_1 = require("./log");
// this is a requirement of whisper.cpp
exports.EXPECTED_SAMPLE_RATE = 16000;
let context;
const getAudioContext = () => {
    if (!context) {
        context = new AudioContext({
            sampleRate: exports.EXPECTED_SAMPLE_RATE,
        });
    }
    return context;
};
const audioDecoder = async (audioBuffer) => {
    const offlineContext = new OfflineAudioContext(audioBuffer.numberOfChannels, audioBuffer.length, audioBuffer.sampleRate);
    const source = offlineContext.createBufferSource();
    source.buffer = audioBuffer;
    source.connect(offlineContext.destination);
    source.start(0);
    const renderedBuffer = await offlineContext.startRendering();
    return renderedBuffer.getChannelData(0);
};
const resampleTo16Khz = async ({ file, onProgress, logLevel = 'info', }) => {
    log_1.Log.info(logLevel, `Starting resampling for file, size: ${file.size}`);
    onProgress === null || onProgress === void 0 ? void 0 : onProgress(0);
    if (typeof window === 'undefined') {
        log_1.Log.error(logLevel, 'Window object not found. Resampling can only be done in a browser environment.');
        throw new Error('Window object not found. Resampling requires a browser environment.');
    }
    if (!file) {
        log_1.Log.error(logLevel, 'File is empty.');
        throw new Error('File is empty');
    }
    const innerContext = getAudioContext();
    const reader = new FileReader();
    return new Promise((resolve, reject) => {
        reader.onprogress = (event) => {
            if (event.lengthComputable) {
                const percentage = (event.loaded / event.total) * 0.5; // File reading up to 50%
                onProgress === null || onProgress === void 0 ? void 0 : onProgress(Math.min(0.5, percentage));
            }
        };
        reader.onload = async () => {
            try {
                log_1.Log.info(logLevel, 'File reading complete. Decoding audio data...');
                onProgress === null || onProgress === void 0 ? void 0 : onProgress(0.5);
                const buffer = new Uint8Array(reader.result);
                const audioBuffer = await innerContext.decodeAudioData(buffer.buffer);
                log_1.Log.info(logLevel, 'Audio decoding complete. Starting rendering...');
                onProgress === null || onProgress === void 0 ? void 0 : onProgress(0.75);
                const processedAudio = await audioDecoder(audioBuffer);
                log_1.Log.info(logLevel, 'Audio resampling and processing complete.');
                onProgress === null || onProgress === void 0 ? void 0 : onProgress(1);
                resolve(processedAudio);
            }
            catch (error) {
                log_1.Log.error(logLevel, 'Error during audio processing:', error);
                reject(error);
            }
        };
        reader.onerror = () => {
            log_1.Log.error(logLevel, 'File reading failed.');
            reject(new Error('File reading failed'));
        };
        reader.readAsArrayBuffer(file);
    });
};
exports.resampleTo16Khz = resampleTo16Khz;
